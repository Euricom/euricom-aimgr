import chalk from "chalk";

export async function handleProviderDetails(providerName?: string) {
  const provider = providerName || "openai";

  console.log(chalk.blue("\nProvider Details:"));
  console.log("yet to implement");
}
